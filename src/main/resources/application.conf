kafkaConsumer {
  dialog {
    topic = helsearbeidsgiver.dialog
  }
}

arbeidsgiver {
  apiBaseUrl = "https://sykepenger-api.ekstern.dev.nav.no"
  apiBaseUrl = ${?NAV_ARBEIDSGIVER_API_BASEURL}

  apiSwaggerUrl = "https://sykepenger-api.ekstern.dev.nav.no/swagger"
  apiSwaggerUrl = ${?NAV_ARBEIDSGIVER_API_SWAGGER_URL}
}
